<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xy.community.dao.QuestionDao">
    <select id="list" resultType="com.xy.community.model.Question">
        select * from question limit #{offset},#{size}
    </select>
    <select id="listByUserId" resultType="com.xy.community.model.Question">
        select * from question where creator=#{id} limit #{offset},#{size}
    </select>
</mapper>

